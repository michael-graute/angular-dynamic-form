import { FormElementHostDirective } from './form-element-host.directive';

describe('FormElementHostDirective', () => {
  it('should create an instance', () => {
//    const directive = new FormElementHostDirective();
//    expect(directive).toBeTruthy();
  });
});
